const autoprefixer = require("autoprefixer");
const tailwindConfig = require("./tailwind.config");




module.exports = {
    Plugins: {
        tailwindConfig: {},
        autoprefixer: {},
    },
};
